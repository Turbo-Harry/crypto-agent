# ============ 密钥（严禁入库） ============
okx_config.json
*.pem
*.key

# ============ 依赖目录（体积大，requirements.txt 现场安装） ============
lib/
node_modules/

# ============ Python ============
__pycache__/
*.pyc
*.pyo
.pycache_tmp/
*.egg-info/
.venv/
venv/

# ============ 运行时状态（活体进程生成，不入库） ============
*.pid
heartbeat_*.txt
watchdog_state.json
trade_journal.json
watchlist.json
positions_snapshot.json
position_ownership.json
position_ownership.lock
engine.lock
threshold_state*.json
experience_scored.json
arb_positions.json
paper_state.json
factor_top.json
crypto_agent.db
crypto_agent.db-wal
crypto_agent.db-shm

# ============ 数据缓存/本地库 ============
data/cache*/
data/cache_*/
data/market.db
*.db
*.sqlite3

# ============ 系统 ============
.DS_Store
*.log

# ============ 飞书通知 CLI（含密钥，不入库） ============
lark-cli-local/
.lark-home/
.lark-config/

# 运行时进度文件(引擎每拍写入)
tick_directional.txt
heartbeat_directional.txt

# ============ 双实例运行时文件（2026-08-23 模拟盘/实盘并行） ============
*.db-shm
*.db-wal
engine_paper.lock
tick_paper.txt
evolution_gate_threshold_paper.json
backups/legacy_json*/
