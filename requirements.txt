# Crypto Agent Python 3.12 运行时依赖（安装到隔离 .venv）
# 初始化：python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
# 旧 lib/ 是 Python 3.9 构建产物；升级期间不覆盖，避免影响仍在运行的旧进程。

# 服务端框架
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# 实时行情 WebSocket
websocket-client>=1.7

# 当前交易/实时行情后端（ccxt 包同时提供 ccxt.pro）
ccxt>=4.5,<5

# 数值/机器学习（决策层与因子研究）
numpy>=1.26

# Agent Harness（LangChain 结构化输出 + LangGraph 唯一编排运行时）
langchain>=1.0,<2
langgraph>=1.0,<2

# 回测/因子研究（可选：仅研究脚本需要）
scipy>=1.11
scikit-learn>=1.3
gplearn>=0.4

# 服务端接口测试
httpx>=0.27
