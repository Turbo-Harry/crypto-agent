# Crypto Agent 运行时依赖（pip install -r requirements.txt --target ./lib）
# 说明：lib/ 目录不入库（体积大+版本随环境漂移），按本清单现场安装。
# 网络安装：pip install -r requirements.txt --target ./lib

# 服务端框架
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# 实时行情 WebSocket
websocket-client>=1.7

# 数值/机器学习（决策层与因子研究）
numpy>=1.26

# 回测/因子研究（可选：仅研究脚本需要）
scipy>=1.11
scikit-learn>=1.3
gplearn>=0.4

# 服务端接口测试
httpx>=0.27
