# 加密货币日内多周期共振择时框架 — 设计文档 v1.0

> 目标：**找最佳入场时机**（多重信号共振的高置信度时刻才入场），不是预测涨跌。
> 哲学：**宁可错过、不可做错**。默认空仓，入场是稀有事件。
> 数据源：OKX/币安，Python。时间框架：`4H → 1H → 15m`。
> 本文所有规则给出**可编码的阈值与参数**，可直接落入 `crypto-agent/config.py` + `strategy/`。

---

## 0. 框架总览

```
                 ┌─────────────────────────────────────────────┐
                 │  硬门(任一门不通过 → 空仓)                     │
                 └─────────────────────────────────────────────┘
  4H 定方向  ──▶  D = 方向评分 ∈ [-100,+100]
                 |D| ≥ 60 且 ADX(14) ≥ 20  →  得到方向 dir=±1
  1H 找结构  ──▶  S = 结构信号(回踩/突破回抽/关键位) + 评分 0-100
                 结构门槛通过 → 得到入场区/参考价位
  15m 精触发 ──▶  T = 触发(限价+拒绝K线) + 追高过滤 + 评分 0-100
                 触发确认 → 得到 入场价/止损价/目标价
  共振评分  ──▶  score = 0.40·|D| + 0.30·S + 0.20·T + 0.10·C
                 score ≥ 75 → 全仓入场；< 60 → 放弃
```

执行节奏：**只在 K 线收盘后评估，下一根开盘/限价单执行**（杜绝未来函数与盘中噪音）。

数据流（与现有 `fetch_okx.py` 对齐）：
- K 线统一为升序 dict 列表：`{open_time, open, high, low, close, volume, quote_volume}`
- OKX `history-candles` 的 `bar` 参数：`"4H"` / `"1H"` / `"15m"`
- 4H 对齐 UTC `00/04/08/12/16/20`，1H 对齐整点，15m 对齐 `00/15/30/45`

---

## 1. 4H 方向过滤（只做顺势方向）

用一个 **方向评分 D ∈ [-100, +100]** 代替单指标判断，5 个分量各有权重，避免单指标误判。

### 1.1 分量（全部基于 4H 收盘 K 线）

| # | 分量 | 规则 | 分数 |
|---|------|------|------|
| 1 | EMA 排列 | 收盘 > EMA50 > EMA200 | **+30** |
|   |          | EMA50 > EMA200 但 收盘 ≤ EMA50（趋势内回踩） | +15 |
|   |          | 收盘 < EMA50 < EMA200 | **−30** |
|   |          | EMA50 < EMA200 但 收盘 ≥ EMA50 | −15 |
| 2 | Supertrend(10, 3) | 多头（价格在趋势线上方） | **+25** |
|   |                    | 空头 | −25 |
| 3 | Donchian(20) 通道 | 收盘 ≥ (20日最高 + 20日最低)/2（通道上半区） | **+15** |
|   |                    | 否则（下半区） | −15 |
| 4 | 摆动结构 | 最近 3 个摆动点（ZigZag 5% 或分形）呈 HH + HL（高抬高、低抬高） | **+20** |
|   |          | 呈 LH + LL | −20 |
|   |          | 混合/无法判定 | 0 |
| 5 | ADX(14) 趋势 | ADX > 25 且 +DI > −DI | **+10** |
|   |              | ADX > 25 且 −DI > +DI | −10 |
|   |              | ADX ≤ 25 | 0 |

### 1.2 判决规则

```
D = 分量之和（范围 -100 ~ +100）
dir = sign(D)                     # +1 多头, -1 空头

硬门1: ADX(14, 4H) < 20      → 空仓（无趋势/震荡市，禁入）
硬门2: |D| < 60              → 空仓（方向不明确）
通过 → dir 确定，只允许做 dir 方向（做多: dir=+1；做空: dir=-1）
```

> 为什么是 `|D|≥60`：D 的分量里，EMA 排列+Supertrend+Donchian 三者同时一致即为 +70（强趋势），
> 允许个别分量（如结构未完全展开、ADX 略弱）缺失但仍 ≥60，兼顾鲁棒与严格。

---

## 2. 1H 信号生成（确认入场结构）

在 4H 顺势方向 `dir` 下，1H 上找**入场结构**。三种结构按优先级：

| 优先级 | 结构 | 本质 |
|---|---|---|
| 1（主） | **回踩**（顺大逆小） | 顺势方向里的健康回调，回到均线/Fib 支撑再接 |
| 2（次） | **突破回抽** | 箱体放量突破后，回踩突破位不破再接 |
| 3（再次） | **关键位反转** | 回踩 4H 级别关键位出现反转结构 |

> 依据现有回测结论（`backtest/report.md`）：**"突破后回踩确认入场"具备样本外有效性**，
> 均值回归在币圈失效。因此主信号必须是"回踩确认"而非"突破追高"。

### 2.1 各结构硬性门槛（做多示例，做空完全对称）

**S1 回踩（主信号）**
1. 1H 收盘 > EMA200(1H)（与 4H 多头方向一致）
2. 价格从近 24 根 1H 的摆动高点回撤，触及以下**任一**：
   - EMA20(1H) 或 EMA50(1H)
   - 或最近一波上涨（显著摆动低→高）的 **0.382–0.5** Fib 回撤位
3. 结构完整：回踩低点 **未跌破** 前一个 1H 摆动低点（不破前低）
4. 1H RSI(14) 回落到 **40–55**（强势回调，非崩溃性下跌）
5. 出现**拒绝 K 线**：下影线 ≥ 2×实体，或阳包阴

**S2 突破回抽（次信号）**
1. 近 20 根 1H 形成箱体（振幅 1%~5%）
2. 放量突破箱体上沿：突破 K 线 `volume > 1.5 × 20根均量`
3. 突破后回踩突破位（箱体上沿 ±0.1×ATR），**未跌回箱体内部**
4. 回踩出现拒绝 K 线

**S3 关键位反转（再次）**
1. 价格回踩 4H 级别关键位：4H EMA20 / 4H 前高突破后的回踩位 / 4H 大级别支撑
2. 触及关键位 ±0.2×ATR(1H)
3. 出现 1H 反转结构（拒绝 K 线或吞没）

### 2.2 1H 信号评分 S ∈ [0, 100]

| 维度 | 规则 | 分数 |
|---|---|---|
| 结构类型 | 回踩 | **40** |
|          | 突破回抽 | 30 |
|          | 关键位反转 | 30 |
| 回撤/回踩质量 | 回踩：Fib 0.382–0.618 | +20 |
|                | 回踩：Fib 0.618–0.786 | +12 |
|                | 回踩：Fib < 0.382（太浅） | +5 |
|                | 回踩：Fib > 0.786（结构危险） | 0（并标记警告） |
|                | 突破回抽：回踩突破位且未破（留 0.1×ATR 缓冲） | +20 |
|                | 突破回抽：轻微跌回但快速收回 | +8 |
|                | 关键位：触及关键位 ±0.2×ATR 精准 | +20 |
|                | 关键位：略越位但收回 | +8 |
| 拒绝 K 线质量 | 强（影线 ≥ 2×实体，收盘回到区上沿） | +20 |
|                | 中 | +12 |
|                | 无 | 0 |
| 与 4H 共振 | 回踩位 ≈ 4H EMA20 / 4H 前高 / 4H Supertrend 线 | +10 |
| 1H RSI 健康 | 多头回调 40–55（空头反弹 45–60） | +10 |

> 满分 = 40 + 20 + 20 + 10 + 10 = 100。结构门槛不通过 → 无信号（S 不存在，直接空仓）。

---

## 3. 15m 精确触发（找最佳入场点，避免追高追低）

1H 给出"该在哪个价位/结构入场"，15m 负责"用限价单 + 拒绝 K 线精确卡点，绝不追价"。

### 3.1 触发算法（做多示例）

1. **定义入场区**：将 1H 信号价位（回踩低点 / 突破位 / EMA 值）映射到 15m，带容忍带 `±0.1×ATR(14,15m)`。
2. **等回抽进区**：等 15m 最低价 ≤ 区上沿（价格真正回到入场区）。
3. **触发确认**：15m 出现**拒绝 K 线**且收盘在区内：
   - 锤子线：下影 ≥ 2×实体；或阳包阴
4. **限价入场**：挂**限价单**在触发 K 线最高价 + 1 tick（突破确认点），**不用市价追**。
   若未成交，则取消并等下一次触发。

### 3.2 追高过滤（宁可错过，任一触发 → 放弃本次）

| 过滤 | 规则 | 目的 |
|---|---|---|
| 延伸过滤 | 触发 K 线收盘距 EMA20(15m) 已 > **0.75×ATR(15m)**（顺势方向） | 价格已延伸，不追 |
| 末端长阳过滤 | 触发 K 线实体 > **1.5×ATR(15m)**（异常放量长阳） | 疑似动能末端 |
| R:R 过滤 | (目标价 − 入场价) < **2 ×** (入场价 − 止损价) | 盈亏比不够，宁可放弃 |

> 核心手法：**只挂限价单、只在回抽进区后成交**——从机制上根除"市价追高/追低"。

### 3.3 15m 触发评分 T ∈ [0, 100]

| 维度 | 规则 | 分数 |
|---|---|---|
| 汇合度（confluence） | 入场区与 VWAP / EMA50(15m) / 前摆动低点 重合个数 × 15 | 最多 +45 |
| 拒绝 K 线质量 | 强 | +25 |
|                | 中 | +15 |
|                | 无 | 0 |
| 未追高 | 收盘距 EMA20(15m) ≤ 0.5×ATR | +20 |
|         | 0.5–0.75×ATR | +10 |
|         | > 0.75×ATR | 直接放弃（过滤） |
| 量能确认 | 触发 K 线 volume > 1.2 × 20根均量 | +10 |

---

## 4. 共振评分（0-100 置信度）

### 4.1 语境分 C ∈ [0, 100]

| 维度 | 规则 | 分数 |
|---|---|---|
| 新闻过滤 | 高影响事件（CPI/FOMC/非农/利率决议，用经济日历）前后 ±30min | 有 → C=0 且**硬过滤** |
| 波动率 | ATR(14,15m) 相对其 288 根（20 天）均值在 0.7–1.5× | +25 |
|         | 1.5–2.0× | +10 |
|         | > 2.0×（异常事件） | 0 |
|         | < 0.7×（无动能） | +5 |
| 相关资产同向 | BTC/ETH 15m 方向一致 | +25 |
| 量能背景 | 15m 近 20 根均量 > 前 20 根均量（动能上升） | +25 |
| 盘口质量 | 买卖价差 < 0.1% | +25 |

### 4.2 最终共振分

```
score = 0.40 × |D|  +  0.30 × S  +  0.20 × T  +  0.10 × C
```

| score | 动作 |
|---|---|
| **≥ 75** | 全仓入场（标准 1R 风险） |
| 60–74 | 半仓（0.5R）——默认**关闭**，贯彻"宁缺毋滥" |
| **< 60** | 不交易 |

> 权重逻辑：4H 方向（0.40）是最大权重（顺势是根基），1H 结构（0.30）次之，
> 15m 触发（0.20）决定时机精度，语境（0.10）做最后校验。
> `score ≥ 75` 意味着：强趋势方向 + 高质量回踩结构 + 高汇合触发 三者必须同时到位。

---

## 5. 止损止盈（ATR 动态 + R:R + 移动止盈）

### 5.1 止损（做多示例，做空对称）

```
ATR14 = ATR(14, 15m)
结构止损 = 触发摆动低点 − 0.2 × ATR14
ATR止损  = 入场价 − 1.5 × ATR14
最终止损 = min(结构止损, ATR止损)          # 取更远/更保守的价位

硬约束：
  止损距离 = (入场价 − 最终止损) / 入场价
  0.3% ≤ 止损距离 ≤ 1.5%（主流 BTC/ETH）；山寨上限放宽到 2.0%
  超过上限 → 波动太大，放弃本次（保持 R 恒定）
```

### 5.2 止盈（分批 + 移动）

| 阶段 | 规则 |
|---|---|
| 入场前提 | **R:R ≥ 2.0**，否则放弃（TP1 距入场 ≥ 2×止损距离） |
| TP1 = 入场 + 1.5R | 平 **50%** 仓位 |
| TP2 = 入场 + 3.0R | 平 **30%** 仓位 |
| 剩余 20% | 移动止盈（见下） |

**移动止盈（只升不降，做多）：**
1. 价格到 1.5R 后 → 止损移到**保本**（入场价）
2. 价格到 2R 后 → 启动追踪，用二者中较优者：
   - Chandelier：`追踪止损 = 持仓期间最高收盘价 − 3×ATR(14,15m)`
   - 或 15m Supertrend(10,3) 翻转为空头时退出
3. 时间止盈：持仓 ≥ **32 根 15m（8 小时）** 仍未触发 TP1 → 全平（或至少平 50%）
4. 事件强平：高影响事件前 30 分钟全平（或减半）

> R 的单位是"止损距离"，不是固定百分比——这样 ATR 变动时盈亏比恒定，代码里用 `R = 止损距离` 归一化。

---

## 6. 风控

| 参数 | 值 | 说明 |
|---|---|---|
| 单笔风险 | **0.75%**（可配置 0.5%–1.0%） | 每笔最多亏账户的 0.75% |
| 仓位公式 | `size = (equity × 0.75%) / 止损距离` | 与现有 `risk_manager.position_size` 同构 |
| 单日最大亏损 | **2.0%** → 当日停手 | 账户级熔断 |
| 连续亏损 | **3 笔** → 冷却 2 小时（或当日停手） | 防情绪化连错 |
| 最大同时持仓 | **1** | 聚焦最佳时机，不做多币摊薄 |
| 每日最大交易次数 | **5** | 防过度交易 |
| 持仓时间上限 | **8 小时** | 日内短线的硬边界 |
| 新闻过滤 | 高影响事件 ±30min 不新开仓 | 见 §4.1 |

> 复用现有 `RiskManager`：新增 `daily_loss_limit=0.02`、`consecutive_loss_limit=3`、`max_daily_trades=5`、`max_hold_bars=32`（15m）。

---

## 7. 完整执行流程（伪代码）

```python
def evaluate_15m_close(klines_4h, klines_1h, klines_15m, ctx):
    # ---- 1. 4H 方向 ----
    D = direction_score(klines_4h)              # §1.1
    adx4h = adx(klines_4h, 14)
    if adx4h < 20 or abs(D) < 60:               # 硬门
        return NO_TRADE("4H 无趋势/方向不明")
    dir = +1 if D > 0 else -1

    # ---- 2. 1H 结构 ----
    sig = signal_1h(klines_1h, dir)             # §2.1 返回结构+参考价, None=无
    if sig is None:
        return NO_TRADE("1H 无结构")

    # ---- 3. 15m 触发 ----
    trig = trigger_15m(klines_15m, dir, sig.ref_price)   # §3.1
    if trig is None or trig.chase_violation:    # §3.2 追高过滤
        return NO_TRADE("15m 未触发/已延伸")
    entry, stop, tp1, tp2 = trig.entry, trig.stop, trig.tp1, trig.tp2
    if (tp1 - entry) < 2 * (entry - stop):      # R:R 过滤
        return NO_TRADE("R:R < 2")

    # ---- 4. 共振评分 ----
    S = sig.score
    T = trig.score
    C = context_score(ctx, klines_15m)          # §4.1
    score = 0.40*abs(D) + 0.30*S + 0.20*T + 0.10*C
    if score < 75:
        return NO_TRADE(f"score={score:.0f} < 75")

    # ---- 5. 风控 + 下单 ----
    if not risk.can_trade() or risk.daily_lost() or risk.consecutive_lost():
        return NO_TRADE("风控熔断")
    size = risk.position_size(equity, stop)     # §6
    return PLACE_LIMIT(dir, entry, stop, tp1, tp2, size)
```

---

## 8. 工程落地要点（对接现有 crypto-agent）

### 8.1 数据与对齐
- OKX `history-candles` `bar` 取值：`"4H"` `"1H"` `"15m"`；币安用 `"4h"` `"1h"` `"15m"`。
- 统一按 UTC 对齐，4H 固定落在 `00/04/08/12/16/20`（避免交易所 bar 起始差异导致回测偏差）。
- 单一交易所单一边（现货或永续）回测，不要混用；永续需计入资金费率。

### 8.2 指标（在 `strategy/indicators.py` 上扩展）
- 已有：`ema`、`sma`、`atr`（返回单值）、`detect_box`、`relative_strength`
- 需新增（纯函数，输入统一升序 dict 列表）：
  `atr_series(klines,14)`、`rsi(closes,14)`、`adx(klines,14)`（含 +DI/−DI）、
  `supertrend(klines,10,3)`、`donchian(klines,20)`、`vwap(klines, session)`、
  `zigzag_swings(klines, 5%)`（或分形 `fractals`）、`fib_levels(swing_low, swing_high)`

### 8.3 参数集中（`config.py` 新增 MTF 段）
所有阈值进 `config.py`，与现有风格一致（见 §10 总表），便于参数扫描与训练/测试分离。

### 8.4 信号评估时序（防未来函数，沿用现有引擎纪律）
- 全部在 K 线**收盘后**用 `[0, t]` 数据评估；入场在下一根开盘，限价单按触发价成交判定。
- 出场用**持仓周期自身**的 15m high/low 判断是否触及止损/止盈（与 `backtest/engine.py` 一致）。

---

## 9. 回测与验证目标

| 指标 | 目标 | 备注 |
|---|---|---|
| 样本量 | ≥ 200 笔 | 少于 200 笔结论统计不显著 |
| 胜率 | 40%–50% | 与 R:R ≥ 2 匹配 |
| 平均盈利 / 平均亏损 | ≥ 2.0 | 盈亏比门槛 |
| 每笔期望值 | ≥ 0.2R | 期望正且稳健 |
| 最大回撤 | < 10% | 日内框架回撤红线 |
| 成本 | 计入 taker 0.05% + 滑点 0.05%（单边）；永续加资金费 ~0.01%/8h | 沿用现有 `FEE_RATE/SLIPPAGE` |

**防过拟合纪律（沿用 `train_test.py`）：**
1. 固定本表默认参数，训练/测试分离评估（不调参）。
2. 覆盖牛/熊/震荡三个样本段。
3. 样本外优于样本内才视为有效；参数响应剧烈非线性（如 21 天好、28 天坏）→ 判为过拟合，回到结构改进而非调参。

---

## 10. 参数总表（可直接写入 `config.py`）

```python
# ============ 4H 方向 ============
DIR_EMA_FAST, DIR_EMA_SLOW = 50, 200
SUPERTREND_PERIOD, SUPERTREND_MULT = 10, 3.0
DONCHIAN_PERIOD = 20
DIR_SCORE_MIN = 60          # |D| 门槛
ADX_MIN = 20                # 4H ADX 趋势门槛
ADX_TREND_LEVEL = 25        # ADX 方向加分线

# ============ 1H 结构 ============
PULLBACK_LOOKBACK = 24      # 摆动高点回看根数
FIB_IDEAL = (0.382, 0.618)  # 理想回撤区
FIB_WARN = 0.786            # 危险回撤线
RSI_HEALTHY = (40, 55)      # 多头回调 RSI 区间
BOX_BARS, BOX_AMP = 20, (0.01, 0.05)
BREAK_VOL_MULT = 1.5

# ============ 15m 触发 ============
ENTRY_ZONE_TOL = 0.1        # 入场区容忍带(×ATR)
REJECT_WICK_RATIO = 2.0     # 拒绝K线下影/实体比
CHASE_MAX_DIST = 0.75       # 距 EMA20 延伸上限(×ATR)
BODY_MAX_ATR = 1.5          # 末端长阳实体上限(×ATR)
TRIGGER_VOL_MULT = 1.2

# ============ 共振评分 ============
W_DIR, W_SIG, W_TRIG, W_CTX = 0.40, 0.30, 0.20, 0.10
SCORE_FULL, SCORE_HALF = 75, 60
HALF_SIZE_ENABLED = False   # 半仓档默认关闭

# ============ 止损止盈 ============
STOP_ATR_MULT = 1.5         # ATR 止损倍数(15m)
STOP_BUFFER_ATR = 0.2       # 结构止损缓冲
STOP_DIST_MIN, STOP_DIST_MAX = 0.003, 0.015   # 主流; 山寨上限 0.02
MIN_RR = 2.0
TP1_R, TP2_R = 1.5, 3.0
TP1_CLOSE, TP2_CLOSE = 0.5, 0.3
TRAIL_ATR_MULT = 3.0        # Chandelier 追踪
TRAIL_START_R = 2.0
MAX_HOLD_BARS = 32          # 8小时(15m)

# ============ 风控(日内) ============
RISK_PER_TRADE_MTF = 0.0075
DAILY_LOSS_LIMIT_MTF = 0.02
CONSEC_LOSS_LIMIT = 3
MAX_DAILY_TRADES = 5
MAX_OPEN_POS = 1
NEWS_BLOCK_MINUTES = 30
```

---

## 附：为什么这样设计（一句话总结每个关键决策）

- **4H 用评分不用单指标**：单指标（如只 EMA 或只 Supertrend）在币圈震荡期误报率高，5 分量评分 + `|D|≥60` 才能"顺势"而非"猜势"。
- **1H 主信号是回踩不是突破**：已有回测证明"回踩确认入场"有样本外有效性，突破追高是亏损主因。
- **15m 只挂限价 + 拒绝 K 线**：机制上杜绝追高追低，入场价天然优于市价单。
- **0-100 共振 + 75 硬门槛**：把"多重信号共振"变成可计算、可回测、可复现的单一分数，宁缺毋滥。
- **R 归一化 + 分批止盈 + 移动追踪**：让 ATR 波动下盈亏比恒定，既保底 1.5R 又让利润奔跑。
